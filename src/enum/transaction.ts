export enum TransactionType {
  STOCK_IN = "STOCK_IN",
  STOCK_OUT = "STOCK_OUT",
}

export enum TransactionReason {
  SALE = "SALE",
  RETURN = "RETURN",
  DAMAGED = "DAMAGED",
  TRANSFER = "TRANSFER",
  ADJUSTMENT = "ADJUSTMENT",
}
